#!/usr/bin/env perl
#use warnings;
#use strict;

$fichpid="lib/.pid";
open (FILE,$fichpid) or die("erreur fichier $fichpid");
while(<FILE>){
    chomp($_);
    kill(SIGTERM,$_);
}
close(FILE);
unlink $fichpid;    
